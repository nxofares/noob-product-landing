/* Reset some default styling */
body, h1, h2, h3, p, ul, li {
  margin: 0;
  padding: 0;
}

/* Apply flexbox to the header */
#header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px;
  background-color: #333;
  color: white;
}

#nav-bar ul {
  list-style: none;
  display: flex;
}

#nav-bar .nav-link {
  text-decoration: none;
  color: white;
  margin: 0 15px;
}

/* Style sections */
section {
  padding: 50px;
}

/* Apply media query for responsiveness */
@media (max-width: 768px) {
  /* Example: Adjust the layout for smaller screens */
  #header {
    flex-direction: column;
    align-items: center;
  }
}
#nav-bar {
  position: fixed;
  top: 0;
  width: 100%;
  background-color: #333;
  z-index: 999;
}
